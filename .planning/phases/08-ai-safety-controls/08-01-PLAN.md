---
phase: 08-ai-safety-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/models/categorizer.go
  - internal/categorizer/categorizer.go
  - internal/categorizer/ai_strategy.go
autonomous: true

must_haves:
  truths:
    - "Category struct carries confidence and source metadata"
    - "Categorizations are logged with confidence before any save operation"
    - "Logs show which strategy produced each categorization"
  artifacts:
    - path: "internal/models/categorizer.go"
      provides: "Category struct with Confidence and Source fields"
      contains: "Confidence  float64"
      min_lines: 15
    - path: "internal/categorizer/categorizer.go"
      provides: "Pre-save logging with confidence"
      contains: "confidence"
      min_lines: 500
    - path: "internal/categorizer/ai_strategy.go"
      provides: "Confidence estimation for AI categorizations"
      contains: "Confidence"
      min_lines: 100
  key_links:
    - from: "internal/categorizer/ai_strategy.go"
      to: "models.Category"
      via: "returns Category with Confidence populated"
      pattern: "Category\\{.*Confidence:"
    - from: "internal/categorizer/categorizer.go"
      to: "logger.Info"
      via: "logs confidence before save"
      pattern: "confidence.*Value.*category\\.Confidence"
---

<objective>
Add confidence metadata infrastructure to enable auditing of AI categorizations.

Purpose: Phase 8 requirement AI-01 depends on knowing which categorizations are high-confidence vs. low-confidence. This plan adds the metadata fields and logging to track confidence scores before auto-learning occurs (which will be gated in Plan 03).

Output: Category struct with confidence and source fields, pre-save logging showing confidence scores for all categorization strategies.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ai-safety-controls/08-RESEARCH.md

# Current implementations
@internal/models/categorizer.go
@internal/categorizer/categorizer.go
@internal/categorizer/ai_strategy.go
@internal/categorizer/direct_mapping_strategy.go
@internal/categorizer/keyword_strategy.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Category struct with confidence metadata</name>
  <files>internal/models/categorizer.go</files>
  <action>
Add two fields to the Category struct (after Description):
- `Confidence float64` - Range 0.0-1.0, representing confidence score
- `Source string` - Strategy that produced this categorization (e.g., "direct_mapping", "keyword", "semantic", "ai")

Update any existing Category constructors/literals in tests to include these fields (set Confidence: 0.9, Source: "test" as sensible defaults).

Rationale: Gemini API doesn't provide explicit confidence scores, so we estimate heuristically. The Source field helps distinguish between high-confidence direct mappings and lower-confidence AI inferences.
  </action>
  <verify>
Run: go build ./internal/models/
Check: `grep -A 3 "type Category struct" internal/models/categorizer.go` shows Confidence and Source fields
  </verify>
  <done>Category struct has Confidence and Source fields; all tests pass after updating constructors</done>
</task>

<task type="auto">
  <name>Task 2: Add confidence estimation to all categorization strategies</name>
  <files>
internal/categorizer/ai_strategy.go
internal/categorizer/direct_mapping_strategy.go
internal/categorizer/keyword_strategy.go
  </files>
  <action>
For each strategy file, update the Categorize() method to populate Category.Confidence and Category.Source:

**direct_mapping_strategy.go:**
- Set `Confidence: 1.0` for successful direct mappings (highest confidence)
- Set `Source: "direct_mapping"`
- If no match found, return uncategorized with `Confidence: 0.0, Source: "none"`

**keyword_strategy.go:**
- Set `Confidence: 0.95` for keyword matches (high but not perfect)
- Set `Source: "keyword"`
- If no match found, pass through to next strategy

**ai_strategy.go:**
- Estimate confidence heuristically:
  - If category name matches known category list: `Confidence: 0.9`
  - If response is empty/uncategorized: `Confidence: 0.0`
  - Otherwise: `Confidence: 0.8` (default AI estimate)
- Set `Source: "ai"`
- Add comment explaining heuristic: "Gemini API does not provide explicit confidence scores; estimated based on response completeness and category match"

Update internal categorizeTransaction() calls to preserve confidence through the strategy chain.
  </action>
  <verify>
Run: go test ./internal/categorizer/ -v -run TestAIStrategy
Run: go test ./internal/categorizer/ -v -run TestDirectMappingStrategy
Run: go test ./internal/categorizer/ -v -run TestKeywordStrategy
Check: All strategies return Category with Confidence > 0 and Source populated
  </verify>
  <done>All three strategies populate Confidence and Source; tests pass; AI strategy includes confidence estimation comment</done>
</task>

<task type="auto">
  <name>Task 3: Add pre-save logging with confidence scores</name>
  <files>internal/categorizer/categorizer.go</files>
  <action>
In the Categorize() method, immediately BEFORE any updateDebitorCategory() or updateCreditorCategory() calls (lines ~508-548 based on RESEARCH.md example), add structured logging:

For debitors:
```go
c.logger.WithFields(
    logging.Field{Key: "party", Value: partyName},
    logging.Field{Key: "category", Value: category.Name},
    logging.Field{Key: "confidence", Value: category.Confidence},
    logging.Field{Key: "source", Value: category.Source},
    logging.Field{Key: "action", Value: "auto_learn_pending"},
).Info("Categorization found (auto-learning enabled)")
```

For creditors: Same structure with "party" value from partyName.

IMPORTANT: Use log level INFO (not DEBUG) so users can audit confidence in production logs. Use "auto_learn_pending" as action (Plan 03 will gate whether save happens).

Also add DEBUG-level log when categorization succeeds but is skipped (e.g., uncategorized result):
```go
c.logger.WithField("party", partyName).Debug("No categorization found, skipping auto-learn")
```
  </action>
  <verify>
Run: go test ./internal/categorizer/ -v -run TestCategorize
Run: CAMT_LOG_LEVEL=info go run . categorize --party="Test Merchant" --is-debtor --amount="50.00" --date="2024-01-01" --info="Test"
Check: Log output includes "confidence" and "source" fields before any save
  </verify>
  <done>Categorize() logs confidence and source at INFO level before update calls; DEBUG log for skipped categorizations; test output shows structured fields</done>
</task>

</tasks>

<verification>
1. Run full categorizer test suite: `go test ./internal/categorizer/ -v`
2. Check Category struct has two new fields: `grep -A 5 "type Category struct" internal/models/categorizer.go`
3. Verify all strategies populate confidence: `grep -r "Confidence:" internal/categorizer/*_strategy.go`
4. Check pre-save logging exists: `grep -A 3 "auto_learn_pending" internal/categorizer/categorizer.go`
5. Run integration test with AI disabled: `make test`
</verification>

<success_criteria>
- [ ] Category struct has Confidence (float64) and Source (string) fields
- [ ] All three strategies (direct_mapping, keyword, ai) populate both fields
- [ ] AI strategy includes comment explaining heuristic estimation
- [ ] Categorizer logs confidence and source at INFO level before any save
- [ ] All existing tests pass with updated Category constructors
- [ ] No behavior changes to actual categorization logic (only metadata addition)
</success_criteria>

<output>
After completion, create `.planning/phases/08-ai-safety-controls/08-01-SUMMARY.md`
</output>
