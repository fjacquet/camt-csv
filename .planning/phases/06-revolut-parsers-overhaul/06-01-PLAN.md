---
phase: 06-revolut-parsers-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/models/transaction.go
  - internal/models/transaction_builder.go
autonomous: true

must_haves:
  truths:
    - "Product field (Current/Savings) appears in Transaction model"
    - "Product field exports to CSV output via csv struct tag"
    - "TransactionBuilder supports WithProduct() method"
  artifacts:
    - path: "internal/models/transaction.go"
      provides: "Product field with csv struct tag"
      min_lines: 50
      contains: "Product.*string.*csv:\"Product\""
    - path: "internal/models/transaction_builder.go"
      provides: "WithProduct() builder method"
      exports: ["WithProduct"]
      contains: "func.*WithProduct"
  key_links:
    - from: "internal/models/transaction.go"
      to: "CSV output"
      via: "csv struct tag"
      pattern: "Product.*`csv:\"Product\"`"
    - from: "internal/models/transaction_builder.go"
      to: "Transaction.Product"
      via: "WithProduct() method"
      pattern: "WithProduct.*Product"
---

<objective>
Add Product field to Transaction model and builder to support Revolut account routing (Current/Savings).

Purpose: Enable distinguishing Revolut Current vs Savings accounts in CSV output for proper iCompta account routing (Current/CHF→Revolut CHF, Savings/CHF→Revolut CHF Vacances, Current/EUR→Revolut EUR).

Output: Product field in Transaction struct with CSV export capability and builder support.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 5 completed the output formatter system
@.planning/phases/05-output-framework/05-03-SUMMARY.md

# Transaction model and builder
@internal/models/transaction.go
@internal/models/transaction_builder.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Product field to Transaction model</name>
  <files>internal/models/transaction.go</files>
  <action>
Add Product field to Transaction struct after Currency field (line ~29):

```go
Product           string          `csv:"Product"`           // Product type (Current, Savings)
```

This field appears in the 35-column CSV output via the csv struct tag. Position after Currency maintains logical grouping of account-level fields.

No changes needed to MarshalCSV() or other methods — gocsv automatically handles the new field via struct tags.
  </action>
  <verify>
Run: `grep -n 'Product.*string.*csv:"Product"' internal/models/transaction.go`

Verify field exists and has csv struct tag for export.
  </verify>
  <done>Product field exists in Transaction struct with proper csv tag, positioned after Currency field.</done>
</task>

<task type="auto">
  <name>Task 2: Add WithProduct() method to TransactionBuilder</name>
  <files>internal/models/transaction_builder.go</files>
  <action>
Add WithProduct() builder method following the existing pattern (after WithType or other string field methods):

```go
// WithProduct sets the product type (e.g., Current, Savings)
func (b *TransactionBuilder) WithProduct(product string) *TransactionBuilder {
    b.transaction.Product = product
    return b
}
```

Follows fluent API pattern used by all other builder methods. No validation needed — accepts any string value from source data.
  </action>
  <verify>
1. Run: `grep -n "func.*WithProduct" internal/models/transaction_builder.go`
2. Run: `make test` to verify no build errors
3. Run: `go test -v ./internal/models/...` to verify tests pass
  </verify>
  <done>WithProduct() method exists in TransactionBuilder, follows fluent API pattern, all tests pass.</done>
</task>

</tasks>

<verification>
1. Product field appears in Transaction struct with csv:"Product" tag
2. TransactionBuilder has WithProduct() method
3. All tests pass (`make test`)
4. Build succeeds (`make build`)
</verification>

<success_criteria>
- Product field is part of Transaction model and exports to CSV via struct tag
- TransactionBuilder supports setting Product via WithProduct()
- No breaking changes to existing code
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-revolut-parsers-overhaul/06-01-SUMMARY.md`
</output>
