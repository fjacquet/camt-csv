---
phase: 10-csv-format-trim
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/formatter/standard.go
  - internal/models/transaction.go
  - internal/formatter/formatter_test.go
autonomous: true

must_haves:
  truths:
    - StandardFormatter produces 29-column CSV output (6 columns removed)
    - MarshalCSV returns 29-element string slice
    - UnmarshalCSV correctly parses 29-column CSV input
    - Removed columns: BookkeepingNumber, IsDebit, Debit, Credit, Recipient, AmountTax
    - iCompta formatter remains unchanged at 10 columns
    - All formatter tests pass with new column count
  artifacts:
    - path: "internal/formatter/standard.go"
      provides: "StandardFormatter with 29-column header"
      min_lines: 40
      exports: ["Header", "Format", "Delimiter"]
    - path: "internal/models/transaction.go"
      provides: "Transaction MarshalCSV/UnmarshalCSV for 29 columns"
      min_lines: 400
      exports: ["MarshalCSV", "UnmarshalCSV"]
    - path: "internal/formatter/formatter_test.go"
      provides: "Tests validating 29-column format"
      min_lines: 250
      contains: "assert.Len(t, header, 29)"
  key_links:
    - from: "internal/formatter/standard.go"
      to: "internal/models/transaction.go"
      via: "Format() calls tx.MarshalCSV()"
      pattern: "tx\\.MarshalCSV\\(\\)"
    - from: "internal/formatter/formatter_test.go"
      to: "internal/formatter/standard.go"
      via: "TestStandardFormatter verifies Header() and Format()"
      pattern: "formatter\\.Header\\(\\)|formatter\\.Format\\("
    - from: "internal/models/transaction.go"
      to: "internal/models/transaction.go"
      via: "MarshalCSV/UnmarshalCSV indices must match"
      pattern: "record\\[\\d+\\]"
---

<objective>
Reduce StandardFormatter CSV output from 35 columns to 29 columns by removing 6 redundant/dead fields.

**Purpose:** Simplify CSV format by eliminating fields that are never populated (BookkeepingNumber, AmountTax) or are redundant with existing fields (IsDebit redundant with CreditDebit, Debit/Credit derived from Amount+CreditDebit, Recipient redundant with Name/PartyName).

**Output:** StandardFormatter producing 29-column CSV output with updated MarshalCSV/UnmarshalCSV methods and passing tests.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Core files for CSV format change
@internal/formatter/standard.go
@internal/models/transaction.go
@internal/formatter/formatter_test.go

# Reference: iCompta formatter (must remain unchanged)
@internal/formatter/icompta.go
</context>

<tasks>

<task type="auto">
  <name>Update StandardFormatter header to 29 columns</name>
  <files>internal/formatter/standard.go</files>
  <action>
    Modify StandardFormatter.Header() to return 29 columns instead of 35 by removing:
    1. BookkeepingNumber (index 0)
    2. IsDebit (index 11)
    3. Debit (index 12)
    4. Credit (index 13)
    5. Recipient (index 19)
    6. AmountTax (index 17)

    New header order (29 columns):
    Status, Date, ValueDate, Name, PartyName, PartyIBAN, Description, RemittanceInfo, Amount, CreditDebit, Currency, Product, AmountExclTax, TaxRate, InvestmentType, Number, Category, Type, Fund, NumberOfShares, Fees, IBAN, EntryReference, Reference, AccountServicer, BankTxCode, OriginalCurrency, OriginalAmount, ExchangeRate

    Update the comment from "35 standard column names" to "29 standard column names".
  </action>
  <verify>
    grep -A 5 "func.*Header.*string" internal/formatter/standard.go
    # Verify header slice has exactly 29 string literals
  </verify>
  <done>StandardFormatter.Header() returns []string with 29 elements matching new column order</done>
</task>

<task type="auto">
  <name>Update Transaction.MarshalCSV to output 29 columns</name>
  <files>internal/models/transaction.go</files>
  <action>
    Modify Transaction.MarshalCSV() (around line 308) to return 29-element []string instead of 35 by removing the same 6 fields.

    Keep the Update method calls at the start (UpdateNameFromParties, UpdateRecipientFromPayee, UpdateDebitCreditAmounts, UpdateInvestmentTypeFromLegacyField) - these maintain internal consistency even though their output fields are removed from CSV.

    New return slice order (29 elements):
    []string{
      t.Status,
      t.formatDateForCSV(t.Date),
      t.formatDateForCSV(t.ValueDate),
      t.Name,
      t.PartyName,
      t.PartyIBAN,
      t.Description,
      t.RemittanceInfo,
      t.Amount.StringFixed(2),
      t.CreditDebit,
      t.Currency,
      t.Product,
      t.AmountExclTax.StringFixed(2),
      t.TaxRate.StringFixed(2),
      t.Investment,
      t.Number,
      t.Category,
      t.Type,
      t.Fund,
      fmt.Sprintf("%d", t.NumberOfShares),
      t.Fees.StringFixed(2),
      t.IBAN,
      t.EntryReference,
      t.Reference,
      t.AccountServicer,
      t.BankTxCode,
      t.OriginalCurrency,
      t.OriginalAmount.StringFixed(2),
      t.ExchangeRate.StringFixed(2),
    }

    DO NOT remove the Update methods - they're still needed for internal consistency.
  </action>
  <verify>
    go test -v -run TestTransactionMarshalCSV ./internal/models/
    # Verify MarshalCSV returns 29 elements (will need test update in next task)
  </verify>
  <done>Transaction.MarshalCSV() returns 29-element []string matching new header order</done>
</task>

<task type="auto">
  <name>Update Transaction.UnmarshalCSV to parse 29 columns</name>
  <files>internal/models/transaction.go</files>
  <action>
    Modify Transaction.UnmarshalCSV() (around line 354) to parse 29-element record instead of 35 by adjusting record indices to match new MarshalCSV output.

    New field assignments (29 elements):
    - t.Status = record[0]           (was record[1], BookkeepingNumber removed)
    - t.Date = parse(record[1])      (was record[2])
    - t.ValueDate = parse(record[2]) (was record[3])
    - t.Name = record[3]             (was record[4])
    - t.PartyName = record[4]        (was record[5])
    - t.PartyIBAN = record[5]        (was record[6])
    - t.Description = record[6]      (was record[7])
    - t.RemittanceInfo = record[7]   (was record[8])
    - t.Amount = parse(record[8])    (was record[9])
    - t.CreditDebit = record[9]      (was record[10])
    - t.Currency = record[10]        (was record[14], IsDebit/Debit/Credit removed)
    - t.Product = record[11]         (was record[15])
    - t.AmountExclTax = parse(record[12]) (was record[16])
    - t.TaxRate = parse(record[13])  (was record[18], AmountTax removed)
    - t.Investment = record[14]      (was record[20], Recipient removed)
    - ... continue with remaining fields matching new indices

    Keep all error handling (fmt.Errorf wrapping).

    DO NOT parse removed fields: BookkeepingNumber, DebitFlag, Debit, Credit, Recipient, AmountTax.
  </action>
  <verify>
    go test -v -run TestTransactionUnmarshalCSV ./internal/models/
    # Verify UnmarshalCSV correctly parses 29-column input
  </verify>
  <done>Transaction.UnmarshalCSV() correctly parses 29-element record with adjusted indices</done>
</task>

<task type="auto">
  <name>Update formatter tests for 29-column format</name>
  <files>internal/formatter/formatter_test.go</files>
  <action>
    Update TestStandardFormatter test cases to expect 29 columns instead of 35:

    1. In "Header returns 35 columns" test (line 101):
       - Change: assert.Len(t, header, 35) → assert.Len(t, header, 29)
       - Change comment: "Header returns 35 columns" → "Header returns 29 columns"
       - Update last column check: header[34] → header[28] (still ExchangeRate)

    2. In "Format single transaction" test (line 114):
       - Change: assert.Len(t, rows[0], 35) → assert.Len(t, rows[0], 29)
       - Update comment: "35 columns (added Product field)" → "29 columns (removed 6 redundant fields)"
       - Update field index assertions to match new order:
         * Status: rows[0][0] (was [1])
         * Date: rows[0][1] (was [2])
         * Name: rows[0][3] (was [4])
         * Amount: rows[0][8] (was [9])
         * Currency: rows[0][10] (was [14])
         * Product: rows[0][11] (was [15])
         * Category: rows[0][16] (was [22])

    3. Remove or update createTestTransaction() to not populate removed fields (BookkeepingNumber, DebitFlag, Debit, Credit, Recipient, AmountTax).

    DO NOT modify iCompta formatter tests - they should remain at 10 columns.
  </action>
  <verify>
    go test -v ./internal/formatter/
    # All StandardFormatter tests pass with 29-column expectations
    # All iComptaFormatter tests still pass unchanged (10 columns)
  </verify>
  <done>All formatter tests pass with StandardFormatter expecting 29 columns and iCompta unchanged at 10 columns</done>
</task>

</tasks>

<verification>
1. Run all formatter tests: `go test -v ./internal/formatter/`
2. Run all model tests: `go test -v ./internal/models/`
3. Verify StandardFormatter.Header() returns exactly 29 strings
4. Verify sample CSV output from any parser shows 29 columns:
   ```bash
   # Example: convert a CAMT sample and count columns
   go run main.go camt --input samples/camt053/sample.xml --output /tmp/test-29col.csv
   head -n 1 /tmp/test-29col.csv | awk -F',' '{print NF}'  # Should output: 29
   ```
5. Verify iCompta formatter still produces 10 columns (unchanged)
</verification>

<success_criteria>
- [ ] StandardFormatter.Header() returns 29-element []string
- [ ] Transaction.MarshalCSV() returns 29-element []string
- [ ] Transaction.UnmarshalCSV() parses 29-element record correctly
- [ ] Removed columns: BookkeepingNumber, IsDebit, Debit, Credit, Recipient, AmountTax
- [ ] All formatter tests pass (go test ./internal/formatter/)
- [ ] All model tests pass (go test ./internal/models/)
- [ ] iCompta formatter unchanged (10 columns, semicolon delimiter)
- [ ] Sample CSV conversion produces 29-column output
</success_criteria>

<output>
After completion, create `.planning/phases/10-csv-format-trim/10-01-SUMMARY.md` documenting:
- Removed columns and their original indices
- New column order (29 columns)
- Test results confirming 29-column format
- Verification that iCompta formatter remains unchanged
</output>
