---
phase: 11-integration-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/camtparser/camtparser_test.go
  - internal/pdfparser/pdfparser_test.go
  - internal/selmaparser/selmaparser_test.go
  - internal/revolutparser/revolutparser_test.go
  - internal/revolutinvestmentparser/revolutinvestmentparser_test.go
  - internal/debitparser/debitparser_test.go
autonomous: true

must_haves:
  truths:
    - "All parser unit tests pass with 29-column CSV format expectations"
    - "Parser tests verify correct header order (Status, Date, ValueDate, ...)"
    - "Parser tests do not reference removed columns (BookkeepingNumber, IsDebit, Debit, Credit, Recipient, AmountTax)"
  artifacts:
    - path: "internal/camtparser/camtparser_test.go"
      provides: "CAMT parser tests with 29-column expectations"
      contains: "expectedCSV.*Status,Date,ValueDate"
    - path: "internal/pdfparser/pdfparser_test.go"
      provides: "PDF parser tests with 29-column expectations"
      contains: "expectedHeaders.*\\[29\\]"
    - path: "internal/selmaparser/selmaparser_test.go"
      provides: "Selma parser tests with 29-column expectations"
      contains: "expectedHeaders.*\\[29\\]"
  key_links:
    - from: "internal/camtparser/camtparser_test.go"
      to: "internal/formatter/standard.go"
      via: "CSV format expectations"
      pattern: "expectedCSV.*Status,Date,ValueDate"
    - from: "internal/pdfparser/pdfparser_test.go"
      to: "internal/formatter/standard.go"
      via: "Header validation"
      pattern: "expectedHeaders"
---

<objective>
Update all parser unit tests to expect the new 29-column CSV format instead of the old 35-column format.

Purpose: Phase 10 reduced StandardFormatter from 35 to 29 columns. Parser tests still have hardcoded expectations for the old format, causing TestConvertToCSV to fail in camtparser and potentially other parsers. This plan updates all parser test files to align with the new format.

Output: All parser unit tests pass with correct 29-column format expectations.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-csv-format-trim/10-01-SUMMARY.md

# Key files defining the new format
@internal/formatter/standard.go
@internal/models/transaction.go

# Test files that need updates
@internal/camtparser/camtparser_test.go
@internal/pdfparser/pdfparser_test.go
@internal/selmaparser/selmaparser_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CAMT parser test for 29-column format</name>
  <files>internal/camtparser/camtparser_test.go</files>
  <action>
Update TestConvertToCSV in camtparser_test.go (line ~186) to expect 29 columns instead of 35:

**Remove these 6 columns from expectedCSV:**
- BookkeepingNumber (position 0)
- IsDebit (position 11)
- Debit (position 12)
- Credit (position 13)
- Recipient (position 19)
- AmountTax (position 17)

**New header order (29 columns):**
Status,Date,ValueDate,Name,PartyName,PartyIBAN,Description,RemittanceInfo,Amount,CreditDebit,Currency,Product,AmountExclTax,TaxRate,InvestmentType,Number,Category,Type,Fund,NumberOfShares,Fees,IBAN,EntryReference,Reference,AccountServicer,BankTxCode,OriginalCurrency,OriginalAmount,ExchangeRate

**Sample data row should remove:**
- First empty field (BookkeepingNumber)
- "true" (IsDebit)
- "100.00" and "0.00" (Debit/Credit)
- "Test Payee" duplicate (Recipient)
- "0.00" after AmountExclTax (AmountTax)

Reference internal/models/transaction_test.go lines 300-350 for working 29-column CSV test data examples.
  </action>
  <verify>go test ./internal/camtparser/ -v -run TestConvertToCSV</verify>
  <done>TestConvertToCSV passes with no CSV format mismatch errors</done>
</task>

<task type="auto">
  <name>Task 2: Update PDF and Selma parser tests for 29-column format</name>
  <files>internal/pdfparser/pdfparser_test.go, internal/selmaparser/selmaparser_test.go</files>
  <action>
Both files have expectedHeaders defined around lines 319 (pdfparser) and 391 (selmaparser). Update these arrays from 35 elements to 29 elements.

**Search for:**
```go
expectedHeaders := []string{
    "BookkeepingNumber", "Status", "Date", ...
}
```

**Replace with 29-column list (remove 6 fields):**
```go
expectedHeaders := []string{
    "Status", "Date", "ValueDate", "Name", "PartyName", "PartyIBAN",
    "Description", "RemittanceInfo", "Amount", "CreditDebit", "Currency",
    "Product", "AmountExclTax", "TaxRate", "InvestmentType", "Number",
    "Category", "Type", "Fund", "NumberOfShares", "Fees", "IBAN",
    "EntryReference", "Reference", "AccountServicer", "BankTxCode",
    "OriginalCurrency", "OriginalAmount", "ExchangeRate",
}
```

If tests check column count explicitly (e.g., `len(headers) == 35`), update to 29.

Copy exact header list from internal/formatter/standard.go Header() method to ensure consistency.
  </action>
  <verify>go test ./internal/pdfparser/ ./internal/selmaparser/ -v</verify>
  <done>Both PDF and Selma parser tests pass with 29-column header expectations</done>
</task>

<task type="auto">
  <name>Task 3: Update remaining parser tests (Revolut, RevolutInvestment, Debit)</name>
  <files>internal/revolutparser/revolutparser_test.go, internal/revolutinvestmentparser/revolutinvestmentparser_test.go, internal/debitparser/debitparser_test.go</files>
  <action>
Search each test file for hardcoded CSV column expectations:
- expectedHeaders arrays with 35 elements
- Column count checks (35)
- Hardcoded CSV strings with 35 columns
- References to removed columns: BookkeepingNumber, IsDebit, Debit, Credit, Recipient, AmountTax

Update any found instances to use 29-column format matching internal/formatter/standard.go Header() method.

If no hardcoded expectations found (tests may use dynamic header reading), verify tests still pass with new format.
  </action>
  <verify>go test ./internal/revolutparser/ ./internal/revolutinvestmentparser/ ./internal/debitparser/ -v</verify>
  <done>All three parser test suites pass without CSV format errors</done>
</task>

<task type="auto">
  <name>Task 4: Run full parser test suite verification</name>
  <files></files>
  <action>
Run complete test suite for all parser packages to verify no regressions:

```bash
go test ./internal/camtparser/ ./internal/pdfparser/ ./internal/selmaparser/ ./internal/revolutparser/ ./internal/revolutinvestmentparser/ ./internal/debitparser/ -v
```

Verify all tests pass. If any failures occur:
1. Check error messages for CSV format mismatches
2. Look for hardcoded 35-column expectations we missed
3. Update those expectations to 29-column format
4. Re-run tests

Also verify formatter tests still pass:
```bash
go test ./internal/formatter/ -v
```
  </action>
  <verify>go test ./internal/camtparser/ ./internal/pdfparser/ ./internal/selmaparser/ ./internal/revolutparser/ ./internal/revolutinvestmentparser/ ./internal/debitparser/ ./internal/formatter/ -v</verify>
  <done>All parser tests pass with zero failures</done>
</task>

</tasks>

<verification>
**Overall Success Checks:**
1. Run `go test ./internal/.../parser/ -v` for all 6 parser packages → all pass
2. Run `go test ./internal/formatter/ -v` → passes
3. Check git diff for any remaining references to removed columns in test files → none found
4. Verify StandardFormatter.Header() returns exactly 29 columns → confirmed
</verification>

<success_criteria>
- [ ] All 6 parser unit test suites pass (CAMT, PDF, Revolut, RevolutInvestment, Selma, Debit)
- [ ] No CSV format mismatch errors in test output
- [ ] No references to removed columns (BookkeepingNumber, IsDebit, Debit, Credit, Recipient, AmountTax) in test assertions
- [ ] Formatter tests continue to pass
- [ ] All expectedHeaders arrays contain exactly 29 elements matching StandardFormatter.Header()
</success_criteria>

<output>
After completion, create `.planning/phases/11-integration-verification/11-01-SUMMARY.md`
</output>
