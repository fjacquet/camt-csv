---
phase: 13-batch-removal-and-format-default
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/common/flags.go
  - CHANGELOG.md
autonomous: true
requirements:
  - FORMAT-01
  - FORMAT-02

must_haves:
  truths:
    - "Running any parser command with no --format flag produces iCompta semicolon-delimited output"
    - "Running --format standard still produces the 29-column comma-delimited CSV"
    - "The --format flag is still available and documented"
  artifacts:
    - path: "cmd/common/flags.go"
      provides: "RegisterFormatFlags with icompta default"
      contains: "icompta"
  key_links:
    - from: "cmd/common/flags.go"
      to: "all parser commands"
      via: "RegisterFormatFlags called in each command's init()"
      pattern: "StringP.*format.*icompta"
---

<objective>
Change the default output format from `standard` to `icompta` so users get iCompta-compatible output without passing any flags.

Purpose: The primary consumer is iCompta. Requiring `--format icompta` on every invocation is friction. Making icompta the default matches real-world use — users who need standard CSV can still pass `--format standard` (FORMAT-02 preserved).
Output: cmd/common/flags.go updated (one-line default change), CHANGELOG.md updated.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change default format to icompta in RegisterFormatFlags</name>
  <files>cmd/common/flags.go</files>
  <action>
    In `cmd/common/flags.go`, update `RegisterFormatFlags`:

    Change:
    ```go
    cmd.Flags().StringP("format", "f", "standard",
        "Output format: standard (35-column CSV) or icompta (iCompta-compatible)")
    ```

    To:
    ```go
    cmd.Flags().StringP("format", "f", "icompta",
        "Output format: icompta (iCompta-compatible, default) or standard (29-column comma-delimited CSV)")
    ```

    This is the only change needed. `RegisterFormatFlags` is called in the `init()` of every parser command (camt, debit, selma, revolut, revolut-investment, pdf), so the default propagates to all commands automatically.

    Do NOT change any test files — test calls that pass `"standard"` explicitly remain valid per FORMAT-02.
  </action>
  <verify>
    1. `go build ./...` exits 0.
    2. `grep 'icompta' cmd/common/flags.go` shows the new default.
    3. `go test ./cmd/common/...` passes.
    4. Build the binary and run `./camt-csv camt --help` — the --format flag description should show `icompta` as default.
  </verify>
  <done>Default format is icompta. All parser commands produce semicolon-delimited iCompta output without any --format flag. --format standard still accepted (FORMAT-02).</done>
</task>

<task type="auto">
  <name>Task 2: Update CHANGELOG.md for Phase 13</name>
  <files>CHANGELOG.md</files>
  <action>
    In `CHANGELOG.md`, add entries under the `## [Unreleased]` section:

    Under **Changed**:
    - "Change default output format from `standard` to `icompta` — iCompta-compatible semicolon-delimited output requires no `--format` flag"

    Under **Removed**:
    - "Remove `batch` subcommand — all parser commands accept folder input directly via auto-detected folder mode"
    - "Remove deprecated `BatchConvertLegacy` internal function from cmd/common"

    Follow the existing CHANGELOG format exactly (imperative mood, present tense). Place entries at the top of the appropriate category sections inside `## [Unreleased]`.
  </action>
  <verify>
    1. `grep "icompta" CHANGELOG.md` returns a match in the Unreleased section.
    2. `grep "batch subcommand" CHANGELOG.md` returns a match in the Unreleased section.
    3. File is valid markdown (no syntax errors visible).
  </verify>
  <done>CHANGELOG.md records both the format default change and the batch removal under [Unreleased].</done>
</task>

</tasks>

<verification>
Run: `make test` (or `go test ./...`).
Build and manual check: `go build -o /tmp/camt-csv-test . && /tmp/camt-csv-test camt --help | grep format` — output must show `(default "icompta")`.
</verification>

<success_criteria>
- No `--format` flag needed for iCompta output (FORMAT-01).
- `--format standard` still works and produces comma-delimited 29-column CSV (FORMAT-02).
- CHANGELOG.md updated.
- `make test` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/13-batch-removal-and-format-default/13-02-SUMMARY.md`
</output>
