---
phase: 13-batch-removal-and-format-default
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - main.go
  - cmd/batch/batch.go
  - cmd/batch/batch_test.go
  - cmd/common/convert.go
autonomous: true
requirements:
  - BATCH-01
  - BATCH-02

must_haves:
  truths:
    - "Running `camt-csv batch` prints an unknown command error"
    - "The cmd/batch package no longer exists in the codebase"
    - "BatchConvertLegacy is removed from cmd/common/convert.go"
    - "All tests pass after removal"
  artifacts:
    - path: "main.go"
      provides: "Root command wiring without batch subcommand"
      contains: "root.Cmd.AddCommand"
    - path: "cmd/common/convert.go"
      provides: "FolderConvert without BatchConvertLegacy"
  key_links:
    - from: "main.go"
      to: "root.Cmd"
      via: "AddCommand calls — batch must be absent"
      pattern: "AddCommand.*batch"
---

<objective>
Remove the `batch` subcommand entirely from the CLI and delete the legacy BatchConvertLegacy function.

Purpose: Phase 12 gave every parser command its own folder-mode path. The standalone `batch` subcommand and the deprecated `BatchConvertLegacy` function are now dead code. Removing them eliminates confusion and satisfies BATCH-01.
Output: cmd/batch/ package deleted, main.go no longer imports or registers batch.Cmd, BatchConvertLegacy removed from cmd/common/convert.go.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete cmd/batch package and deregister from main.go</name>
  <files>cmd/batch/batch.go, cmd/batch/batch_test.go, main.go</files>
  <action>
    1. Delete the entire `cmd/batch/` directory (both batch.go and batch_test.go).
    2. In `main.go`:
       - Remove the import `"fjacquet/camt-csv/cmd/batch"`.
       - Remove the line `root.Cmd.AddCommand(batch.Cmd)` from the `init()` function.
    3. Run `go build ./...` to confirm no compilation errors.
    Note: Do NOT add any deprecation message or replacement hint — the command simply disappears (unknown command error from Cobra is the desired UX per BATCH-01).
  </action>
  <verify>
    1. `go build ./...` exits 0.
    2. `ls cmd/batch/` returns "no such file or directory".
    3. `grep -r "cmd/batch" main.go` returns no matches.
  </verify>
  <done>The batch package is gone. main.go compiles cleanly without it. Running `camt-csv batch` would produce Cobra's "unknown command" error.</done>
</task>

<task type="auto">
  <name>Task 2: Remove BatchConvertLegacy from cmd/common/convert.go</name>
  <files>cmd/common/convert.go</files>
  <action>
    In `cmd/common/convert.go`:
    - Delete the `BatchConvertLegacy` function and its doc comment block (lines starting with "// BatchConvertLegacy processes all files..." through the closing brace).
    - Remove the `"encoding/json"` import if it is only used by BatchConvertLegacy. Scan the remaining code — if `json.Unmarshal` appears nowhere else, drop that import.
    - Run `go build ./...` and `go vet ./...` to confirm no unused imports or references remain.
    Note: `FolderConvert` remains — it is the active replacement. Only `BatchConvertLegacy` is removed.
  </action>
  <verify>
    1. `go build ./...` exits 0.
    2. `go vet ./...` exits 0.
    3. `grep "BatchConvertLegacy" cmd/common/convert.go` returns no matches.
    4. `make test` passes (or `go test ./...` exits 0).
  </verify>
  <done>BatchConvertLegacy is gone. cmd/common/convert.go compiles cleanly. All existing tests pass.</done>
</task>

</tasks>

<verification>
Run the full test suite: `make test` (or `go test ./...`).
Build: `make build`.
Spot-check CLI: build the binary and run `./camt-csv batch` — Cobra must print "unknown command 'batch'" and exit non-zero.
</verification>

<success_criteria>
- `camt-csv batch` produces "unknown command" error (BATCH-01 satisfied).
- `BatchConvertLegacy` does not appear anywhere in the codebase (BATCH-02 legacy cleanup).
- `make test` and `make build` both succeed.
</success_criteria>

<output>
After completion, create `.planning/phases/13-batch-removal-and-format-default/13-01-SUMMARY.md`
</output>
